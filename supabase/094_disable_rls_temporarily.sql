-- ============================================
-- 094: ВРЕМЕННО отключить RLS на user_permissions
-- ============================================

-- ВРЕМЕННО отключить RLS для отладки
ALTER TABLE user_permissions DISABLE ROW LEVEL SECURITY;

-- Это должно разрешить все операции
-- После успешного теста включим обратно и поймем в чем проблема
